<UserControl x:Class="Orión.DesktopUI.Views.ProveedorListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Orión.DesktopUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Padding="16" Margin="0,0,0,16">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <materialDesign:PackIcon Kind="TruckDelivery" Width="32" Height="32" VerticalAlignment="Center" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="GESTIÓN DE PROVEEDORES" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="16,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
                </StackPanel>
                <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="ACTUALIZAR" HorizontalAlignment="Right" Command="{Binding LoadDataCommand}" Width="120"/>
            </DockPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="1" materialDesign:ElevationAssist.Elevation="Dp2">
            <DataGrid ItemsSource="{Binding Proveedores}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                      SelectionMode="Single" SelectedItem="{Binding SelectedProveedor}"
                      Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding IdProveedor}" Width="Auto"/>
                    <DataGridTextColumn Header="NOMBRE" Binding="{Binding Nombre}" Width="*"/>
                    <DataGridTextColumn Header="RUC" Binding="{Binding RUC}" Width="Auto"/>
                    <DataGridTextColumn Header="TELÉFONO" Binding="{Binding Telefono}" Width="Auto"/>
                    <DataGridTextColumn Header="EMAIL" Binding="{Binding Email}" Width="*"/>
                    <DataGridTemplateColumn Header="ESTADO" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <materialDesign:Chip Content="{Binding Activo, Converter={StaticResource BooleanToStatusConverter}}" 
                                                     Background="{Binding Activo, Converter={StaticResource StatusToBrushConverter}}" 
                                                     Foreground="White"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="ACCIONES" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="ESTADO"
                                        Command="{Binding DataContext.ToggleStatusCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" Height="25" Padding="8,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
