<UserControl x:Class="Orión.DesktopUI.Views.SolicitudListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Style="{StaticResource ViewEntranceAnimation}" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="CONTROL DE SOLICITUDES" Style="{StaticResource MaterialDesignHeadline5TextBlock}" VerticalAlignment="Center"/>
            
            <TextBox Width="250" Margin="32,0,0,0"
                     materialDesign:HintAssist.Hint="Buscar solicitud..."
                     materialDesign:TextFieldAssist.HasClearButton="True"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding LoadDataCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <Button Command="{Binding ShowFormCommand}" Style="{StaticResource MaterialDesignRaisedButton}" 
                    Background="{StaticResource SecondaryBrush}" BorderBrush="{StaticResource SecondaryBrush}"
                    Margin="24,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="NUEVA SOLICITUD"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Formulario (Condicional) -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,24" Padding="16"
                             Visibility="{Binding IsFormVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="REGISTRAR FALLA" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,16" FontWeight="Bold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <ComboBox materialDesign:HintAssist.Hint="Seleccionar Máquina"
                                  ItemsSource="{Binding Maquinas}"
                                  SelectedValue="{Binding SelectedMaquinariaId}"
                                  SelectedValuePath="IdMaquinaria"
                                  DisplayMemberPath="NombreMaquina"
                                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,16,0">
                        <ComboBox materialDesign:HintAssist.Hint="Tipo Mantenimiento"
                                  ItemsSource="{Binding TiposMantenimiento}"
                                  SelectedValue="{Binding SelectedTipoManttoId}"
                                  SelectedValuePath="IdTipoMantto"
                                  DisplayMemberPath="DescripcionTipo"
                                  Style="{StaticResource MaterialDesignFloatingHintComboBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="2">
                        <TextBox materialDesign:HintAssist.Hint="Descripción de la Falla"
                                 Text="{Binding DescripcionFalla}"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                    </StackPanel>
                </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="CANCELAR" Command="{Binding HideFormCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
                    <Button Content="GUARDAR" Command="{Binding SaveSolicitudCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

        <ProgressBar Grid.Row="2" IsIndeterminate="True" VerticalAlignment="Top" Panel.ZIndex="1"
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Listado -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Solicitudes}" AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdSS}"/>
                <DataGridTextColumn Header="MÁQUINA" Binding="{Binding NombreMaquinaria}"/>
                <DataGridTextColumn Header="FALLA" Binding="{Binding DescripcionFalla}" Width="*"/>
                <DataGridTextColumn Header="APERTURA" Binding="{Binding FechaApertura, StringFormat=g}"/>
                <DataGridTextColumn Header="TIPO" Binding="{Binding TipoMantenimientoDescripcion}"/>
                <DataGridTextColumn Header="TÉCNICO" Binding="{Binding TecnicoNombre}"/>
                <DataGridTemplateColumn Header="ESTADO">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <materialDesign:Chip Content="{Binding EstadoDescripcion}" 
                                                 Background="{StaticResource PrimaryBrush}" Foreground="White"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="ACCIONES">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="REPORTE" Style="{StaticResource MaterialDesignFlatButton}" 
                                    Command="{Binding DataContext.GenerateReportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

