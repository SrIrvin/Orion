<Window x:Class="Orión.DesktopUI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Gestor Orión - Login" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid>
        <materialDesign:Card Width="350" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center" UniformCornerRadius="10" materialDesign:ElevationAssist.Elevation="Dp8">
            <StackPanel>
                <materialDesign:PackIcon Kind="AccountCircle" Width="80" Height="80" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="INICIAR SESIÓN" Style="{StaticResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center" Margin="0,16,0,32" FontWeight="Bold"/>
                
                <TextBox materialDesign:HintAssist.Hint="Nombre de Usuario" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,16"/>

                <PasswordBox materialDesign:HintAssist.Hint="Contraseña" 
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             Name="PasswordBox"
                             Margin="0,0,0,32"/>

                <Button Content="ENTRAR" IsDefault="True" Command="{Binding LoginCommand}"
                        Click="LoginButton_Click"
                        materialDesign:ButtonProgressAssist.IsIndeterminate="{Binding IsBusy}"
                        materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding IsBusy}"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,16,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
