<Window x:Class="Orión.DesktopUI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:helpers="clr-namespace:Orión.DesktopUI.Helpers"
        Title="Gestor Orión - Login" Height="550" Width="500"
        Icon="../Assets/Icono.ico"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        TextElement.Foreground="{StaticResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Background="{StaticResource BackgroundBrush}">
        <materialDesign:Card Width="380" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center" 
                             UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp12"
                             Background="White">
            <StackPanel>
                <materialDesign:Card Width="120" Height="120" UniformCornerRadius="60" 
                                     Padding="0" Background="White" materialDesign:ElevationAssist.Elevation="Dp6"
                                     HorizontalAlignment="Center" Margin="0,0,0,32">
                    <Image Source="../Assets/Icono.PNG" Width="85" Height="85" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </materialDesign:Card>
                
                <TextBlock Text="INICIAR SESIÓN" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" 
                           HorizontalAlignment="Center" Margin="0,0,0,32" Foreground="{StaticResource TextSecondaryBrush}"
                           FontWeight="SemiBold"/>
                
                <TextBox materialDesign:HintAssist.Hint="Nombre de Usuario" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         materialDesign:TextFieldAssist.PrefixText="👤 "
                         Margin="0,0,0,24"/>

                <materialDesign:Card Background="#F5F7F9" Padding="8,4" UniformCornerRadius="8" Margin="0,0,0,40">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Texto Plano (Visible al presionar) -->
                        <TextBox Grid.Column="0" 
                                 materialDesign:HintAssist.Hint="Contraseña"
                                 materialDesign:TextFieldAssist.PrefixText="🔒 "
                                 Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                                 BorderThickness="0" VerticalAlignment="Center"
                                 Visibility="{Binding ElementName=ShowPassBtn, Path=IsPressed, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- Enmascarado (Oculto al presionar) -->
                        <PasswordBox Grid.Column="0" 
                                     materialDesign:HintAssist.Hint="Contraseña"
                                     materialDesign:TextFieldAssist.PrefixText="🔒 "
                                     helpers:PasswordBoxHelper.BindPassword="True"
                                     helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     BorderThickness="0" VerticalAlignment="Center"
                                     x:Name="ConfigPasswordBox">
                            <PasswordBox.Style>
                                <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFloatingHintPasswordBox}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=ShowPassBtn, Path=IsPressed}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </PasswordBox.Style>
                        </PasswordBox>

                        <Button Grid.Column="1" x:Name="ShowPassBtn"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Foreground="{StaticResource PrimaryBrush}"
                                Width="32" Height="32">
                            <materialDesign:PackIcon Kind="Eye" Width="20" Height="20"/>
                        </Button>
                    </Grid>
                    </materialDesign:Card>

                    <CheckBox Content="Recordarme en este equipo" 
                          IsChecked="{Binding RememberMe}"
                          Margin="0,0,0,24"
                          Foreground="{StaticResource PrimaryBrush}"
                          FontWeight="SemiBold"/>

                    <Button Content="ENTRAR" 
 IsDefault="True" Command="{Binding LoginCommand}"
                        Click="LoginButton_Click" Height="45"
                        materialDesign:ButtonProgressAssist.IsIndeterminate="{Binding IsBusy}"
                        materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding IsBusy}"
                        Background="{StaticResource PrimaryBrush}" BorderBrush="{StaticResource PrimaryBrush}"
                        Foreground="White"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentBrush}" 
                           HorizontalAlignment="Center" Margin="0,16,0,0" TextWrapping="Wrap" FontSize="12"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
