<Window x:Class="Orión.DesktopUI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Gestor Orión - Login" Height="450" Width="400"
        Icon="../Assets/Icono.ico"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        TextElement.Foreground="{StaticResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
        <Grid Background="{StaticResource BackgroundBrush}" Margin="0,0,0,-16">
        <materialDesign:Card Width="350" Padding="32" VerticalAlignment="Center" HorizontalAlignment="Center" 
                             UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp12"
                             Background="White" Height="430">
            <StackPanel>
                <Image Source="../Assets/Logo.jpg" Width="200" Margin="0,0,0,24" HorizontalAlignment="Center">
                    <Image.Effect>
                        <DropShadowEffect BlurRadius="10" Color="Gray" Opacity="0.3" ShadowDepth="2"/>
                    </Image.Effect>
                </Image>
                
                <TextBlock Text="INICIAR SESIÓN" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" 
                           HorizontalAlignment="Center" Margin="0,0,0,32" Foreground="{StaticResource TextSecondaryBrush}"
                           FontWeight="SemiBold" LetterSpacing="2"/>
                
                <TextBox materialDesign:HintAssist.Hint="Nombre de Usuario" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         materialDesign:TextFieldAssist.PrefixText="👤 "
                         Margin="0,0,0,24"/>

                <PasswordBox materialDesign:HintAssist.Hint="Contraseña" 
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             Name="PasswordBox"
                             materialDesign:TextFieldAssist.HasClearButton="True"
                             materialDesign:TextFieldAssist.PrefixText="🔒 "
                             Margin="0,0,0,40"/>

                <Button Content="ENTRAR" IsDefault="True" Command="{Binding LoginCommand}"
                        Click="LoginButton_Click" Height="45"
                        materialDesign:ButtonProgressAssist.IsIndeterminate="{Binding IsBusy}"
                        materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding IsBusy}"
                        Background="{StaticResource PrimaryBrush}" BorderBrush="{StaticResource PrimaryBrush}"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentBrush}" 
                           HorizontalAlignment="Center" Margin="0,16,0,0" TextWrapping="Wrap" FontSize="12"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>
